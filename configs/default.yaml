# configs/default.yaml
defaults:
  - dataset: mini_imagenet
  - _self_

# Global Execution Settings
mode: "train"
seed: 42
device: "cuda"
checkpoint_path: null           # Added at root to support main.py eval routing

# Few-Shot Task Settings
task:
  n_way: 5
  n_shot: 1
  n_query: 15
  train_episodes: 200
  val_episodes: 600
  test_episodes: 2000

# Model Architecture Configuration
model:
  modality: "multimodal"        # Options: 'vision', 'graph', 'multimodal'
  hidden_dim: 128
  node_feat_dim: 11
  edge_feat_dim: 14
  num_layers: 3
  dropout: 0.1
  gnn_type: "GINE"              # Options: 'GINE', 'GATv2', 'ResGated', 'GPS'
  norm_type: "graph"            # Options: 'graph', 'layer', 'batch' (Cai et al., ICML 2021)
  use_dual_pool: true           # Dual-Pooling [Mean || Max] Readout (Xu et al., ICLR 2019)
  fusion_type: "dual_gate"      # Options: 'dual_gate', 'concat', 'add'
  modality_dropout: 0.0         # Options: 0.0 (off) or 0.15 (on)
  fsl_method: "protonet"        # Options: 'protonet', 'matching'
  distance_metric: "cosine"     # Options: 'cosine', 'euclidean'
  use_simpleshot: false

# Training Hyperparameters
training:
  epochs: 100
  lr: 0.001
  weight_decay: 0.0001          # Standard AdamW decoupled weight decay
  save_dir: "experiments/multimodal_dual_gate_5w1s"
  num_workers: 4

# Evaluation Settings
evaluation:
  checkpoint_path: null
  test_episodes: 2000

# =============================================================================
# Option A: Route Hydra outputs directly into training.save_dir
# Prevents creating duplicate 'outputs/' directories on disk
# =============================================================================
hydra:
  run:
    dir: ${training.save_dir}